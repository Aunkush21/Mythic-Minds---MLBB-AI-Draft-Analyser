fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
asyncpg>=0.29
python-dotenv>=1.0
python-multipart>=0.0.9

# Required by ml/ (imported directly by the service layer — see
# app/services/ml_bridge.py for why the backend imports ml/ in-process
# instead of treating it as a separate microservice)
pandas
numpy
scikit-learn
xgboost
shap
joblib
pyarrow

# Testing
pytest>=8.0
httpx>=0.27
